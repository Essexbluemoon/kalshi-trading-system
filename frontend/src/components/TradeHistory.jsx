/**
 * TradeHistory.jsx
 * Panel 4 — filterable, sortable trade log.
 * Filters: date range, category, strategy, won/lost.
 * Implemented in Phase 5.
 */
import React from "react";

export default function TradeHistory() {
  throw new Error("TradeHistory implemented in Phase 5");
}
