/**
 * PortfolioSummary.jsx
 * Panel 1 — four summary cards + cumulative P&L line chart vs benchmark.
 * Implemented in Phase 5.
 */
import React from "react";
import { useApi } from "../hooks/useApi.js";

export default function PortfolioSummary() {
  // Phase 5: const { data: summary } = useApi("/performance/summary");
  // Phase 5: const { data: daily }   = useApi("/performance/daily");
  throw new Error("PortfolioSummary implemented in Phase 5");
}
